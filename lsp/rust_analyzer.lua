return {
    cmd = { 'rust-analyzer' },
    root_markers = { "Cargo.toml", ".git" },
    single_file_support = true,
    settings = {
        -- ['rust-analyzer'] = {
        --     diagnostics = {
        --         enable = false,
        --     }
        -- }
    },

}
